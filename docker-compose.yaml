version: '3'

networks:
  whaticket-premium:


services:

  backend:
    build:
      context: ./backend
      dockerfile: ./Dockerfile
    environment:
      - DB_DIALECT=postgres
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASS=${POSTGRES_PASSWORD:-postgres}
      - DB_NAME=${POSTGRES_DB:-whaticket}
      - DB_READ_HOST=${DB_READ_HOST:-postgres}
      - JWT_SECRET=${JWT_SECRET:-***REMOVED_JWT_SECRET***}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-***REMOVED_JWT_REFRESH_SECRET***}
      - BACKEND_URL=${BACKEND_URL:-http://localhost}:${BACKEND_PORT:-8080}
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:3000}
      - PROXY_PORT=${PROXY_PORT:-8080}
      - CHROME_ARGS=--no-sandbox --disable-setuid-sandbox
      - AMQP_URL=${AMQP_URL:-amqp://***REMOVED_AMQP_CREDENTIALS***@rabbitmq:5672}
      - PORT=8080
    ports:
      - ${BACKEND_PORT:-8080}:8080
    volumes:
      - ./backend/public/:/usr/src/app/public/
      - ./backend/.wwebjs_auth/:/usr/src/app/.wwebjs_auth/
    depends_on:
      - postgres
      - rabbitmq
    networks:
      - whaticket-premium

  frontend:
    ports:
      - ${FRONTEND_PORT:-3000}:80
      - ${FRONTEND_SSL_PORT:-3001}:443
    build:
      context: ./frontend
      dockerfile: ./Dockerfile
    environment:
      - URL_BACKEND=backend:8080
      - VITE_BACKEND_URL=${BACKEND_URL:-http://localhost}:${PROXY_PORT:-8080}/
      - VITE_HOURS_CLOSE_TICKETS_AUTO=${HOURS_CLOSE_TICKETS_AUTO}
      - FRONTEND_SERVER_NAME=${FRONTEND_SERVER_NAME}
      - BACKEND_SERVER_NAME=${BACKEND_SERVER_NAME}
    volumes:
      - ./ssl/certs/:/etc/nginx/ssl/
      - ./ssl/www/:/var/www/letsencrypt/
    networks:
      - whaticket-premium

  postgres:
    image: ronaldodavi/pgvectorgis:latest
    volumes:
      - ./.docker/db_data/:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-whaticket}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - TZ=${TZ:-America/Sao_Paulo}
    ports:
      - ${MYSQL_PORT:-5432}:5432
    restart: always
    networks:
      - whaticket-premium

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: whaticket-premium-rabbitmq
    restart: always
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER:-guest}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASS:-guest}
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - ./.docker/rabbitmq/data:/var/lib/rabbitmq/mnesia/
      - ./.docker/rabbitmq/log:/var/log/rabbitmq
    networks:
      - whaticket-premium

  whaileys-engine:
    container_name: whaticket-premium-engine
    build:
      context: ./engine-standard
      dockerfile: ./Dockerfile
    environment:
      - AMQP_URL=${AMQP_URL:-amqp://***REMOVED_AMQP_CREDENTIALS***@rabbitmq:5672}
    depends_on:
      - rabbitmq
    networks:
      - whaticket-premium
