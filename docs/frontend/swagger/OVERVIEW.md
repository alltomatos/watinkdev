# Swagger

Visualizador integrado da documentação de API do Backend.

## Acesso
- **Rota**: `/swagger` (Frontend) ou `/docs` (Backend direto).
- Permite testar requisições diretamente pelo navegador.
